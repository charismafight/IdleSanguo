using System.Collections;
using System.Collections.Generic;
using Assets.Common;
using UnityEngine;

public class PrefabMakerScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Utility.ShowTips("suck");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
